import styled, {css} from 'styled-components'import stack from '../../../assets/images/stack.svg'export const ImageWrapper = styled.div`  margin-bottom: 14px;  position: relative;  padding-top: 20px;`interface ImageInnerProps {  $mask?: boolean,  $widthMask: boolean,}export const ImageInner = styled.div<ImageInnerProps>`  max-width: 400px;    max-height: 400px;  overflow: hidden;  background-color: #ffffff;  cursor: grab;  position: relative;${({$mask, $widthMask}) => $widthMask && css`  &::before {    content: '';    transition: background-color 0.1s ease, opacity 0.1s ease;    inset: 0;    z-index: 1;    -webkit-mask: url('${stack}#mask') no-repeat center;    position: absolute;    background: ${$mask ? '#ffffff' : '#000000'};    opacity: ${$mask ? 1 : 0.5};    mask-size: 100% 100%;  }`}   img {    display: block;    width: 100%;    height: 100%;    object-fit: contain;    object-position: center;    pointer-events: none;  }`export const YScale = styled.div`  transform: translateY(-50%) rotate(90deg);  position: absolute;  top: 50%;  right: -55px;`export const XScale = styled.div`  position: absolute;  left: 50%;  transform: translateX(-50%);`