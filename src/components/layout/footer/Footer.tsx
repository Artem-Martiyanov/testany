import * as React from 'react';
import {Container, StyledFooter} from "./styles";

const Footer = () => {
  return (
    <StyledFooter>
      <Container>
        Autor: Artem Martiyanov
      </Container>
    </StyledFooter>
  );
};

export default Footer;
