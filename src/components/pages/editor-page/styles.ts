import styled from 'styled-components'


export const EditorWrapper = styled.section`
padding-top: 40px;

`

export const EditorLeftSide = styled.aside`
  width: max(20%, 320px);
  background-color: rgba(255, 0, 0, 0.2);
`

export const PreviewImageBlock = styled.aside`
  width: max(20%, 320px);

`

export const PreviewImageWrapper = styled.div`
  width: 100%;
  overflow: hidden;
`