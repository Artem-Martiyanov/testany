import React, {FC} from 'react';
import {Container} from "../../styled";




const MainPage: FC = () => {
  
  return (
      <Container>
        Главная
      </Container>
  );
};

export default React.memo(MainPage);